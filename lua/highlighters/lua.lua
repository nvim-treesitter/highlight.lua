local PARSER_FILE = 'parser/'

local function highlight(colors)
    -- local tree = vim.treesitter.require_language('javascript', PARSER_FILE)
    -- print(vim.inspect(tree))
end

return {
    highlight = highlight
}

